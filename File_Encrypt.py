import tkinter as tk
from tkinter import filedialog
from tkinter import Button


window = tk.Tk()

#Title of the window
window.title('File Encryption/Decryption')

#default window size
window.geometry('600x400')

#Background color
window.configure(background='dark grey')

#Top label
label = tk.Label(window, text="Encrypt or Decrypt Files", font=("Arial", 16),  bg="dark grey")

#Padding for Label
label.pack(pady=0)











 






















#loop to keep the window open
window.mainloop()
